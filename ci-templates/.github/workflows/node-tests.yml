name: Node Tests

on:
  pull_request:
    branches:
      - production

jobs:
  node-tests:
    uses: alleyinteractive/.github/.github/workflows/node-tests.yml@main
    with:
      ci: true
      node: 18
      run-audit: true
